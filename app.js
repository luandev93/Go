const $=id=>document.getElementById(id);const courses={go:{name:'Aprenda Go',mark:'GO',file:'playlist.csv',key:'aprenda_go_progress_v3'},rust:{name:'Aprenda Rust',mark:'🦀',file:'rust-playlist.csv',key:'aprenda_rust_progress_v1'}};let current='go',data={};function csv(t){const a=[],rows=t.trim().split(/\r?\n/).slice(1);for(const row of rows){let p=[],c='',q=false;for(let i=0;i<row.length;i++){let x=row[i];if(x==='"'){if(q&&row[i+1]==='"'){c+='"';i++}else q=!q}else if(x===','&&!q){p.push(c);c=''}else c+=x}p.push(c);a.push({id:p[0],title:p[2],duration:p[3],seconds:Number(p[4]),url:'https://www.youtube.com/watch?v='+p[1]})}return a}async function load(){for(const [id,c] of Object.entries(courses)){const r=await fetch('./'+c.file,{cache:'no-store'});if(!r.ok)throw Error(c.file);data[id]=csv(await r.text())}refresh()}function doneSet(id=current){try{return new Set(JSON.parse(localStorage.getItem(courses[id].key)||'[]'))}catch{return new Set()}}function save(id,set){localStorage.setItem(courses[id].key,JSON.stringify([...set]))}function fmt(s){s=Math.max(0,Number(s)||0);const h=Math.floor(s/3600),m=Math.floor(s%3600/60),sec=Math.floor(s%60);return h?`${h}h ${m}min`:`${m}min ${String(sec).padStart(2,'0')}s`}function stats(id){const v=data[id]||[],d=doneSet(id),done=v.filter(x=>d.has(x.id)),studied=done.reduce((s,x)=>s+x.seconds,0),total=v.reduce((s,x)=>s+x.seconds,0);return{count:done.length,total:v.length,studied,remaining:total-studied,pct:v.length?Math.round(done.length/v.length*100):0}}function render(){const v=data[current]||[],d=doneSet(),q=$('search').value.toLowerCase().trim(),tbody=$('list');tbody.innerHTML='';v.forEach((x,i)=>{if(q&&!x.title.toLowerCase().includes(q))return;const tr=document.createElement('tr');if(d.has(x.id))tr.className='done';const cb=document.createElement('input');cb.type='checkbox';cb.className='check';cb.checked=d.has(x.id);cb.onchange=()=>{cb.checked?d.add(x.id):d.delete(x.id);save(current,d);refresh()};const a=document.createElement('a');a.textContent=x.title;a.href=x.url;a.target='_blank';a.rel='noopener noreferrer';tr.innerHTML='<td></td><td class="num"></td><td></td><td class="time"></td>';tr.children[0].appendChild(cb);tr.children[1].textContent=i+1;tr.children[2].appendChild(a);tr.children[3].textContent=x.duration;tbody.appendChild(tr)})}function refresh(){if(!data[current])return;const s=stats(current);$('brandMark').textContent=courses[current].mark;$('courseTitle').textContent=courses[current].name;$('count').textContent=`${s.count} / ${s.total}`;$('studied').textContent=fmt(s.studied);$('remaining').textContent=fmt(s.remaining);$('percent').textContent=s.pct+'%';$('bar').style.width=s.pct+'%';render();progress()}function progress(){const s=stats(current);$('progressBig').textContent=s.pct+'%';$('progressTitle').textContent=courses[current].name;$('progressSummary').textContent=`${s.count} de ${s.total} aulas concluídas`;$('progressWide').style.width=s.pct+'%';$('progressDone').textContent=s.count;$('progressTotal').textContent=`de ${s.total} aulas`;$('progressStudied').textContent=fmt(s.studied);$('progressRemaining').textContent=fmt(s.remaining);$('progressLanguage').textContent=current==='go'?'Go':'Rust';const list=$('courseSummaryList');list.innerHTML='';for(const id of Object.keys(courses)){const x=stats(id),item=document.createElement('article');item.className='domain-item';item.innerHTML=`<div class="domain-top"><div><strong>${courses[id].mark} ${courses[id].name}</strong><span>${x.count}/${x.total} aulas · ${fmt(x.studied)} estudados</span></div><b>${x.pct}%</b></div><div class="domain-track"><i style="width:${x.pct}%"></i></div><small>${fmt(x.remaining)} restantes · progresso salvo separadamente</small>`;list.appendChild(item)}}function view(name){if(name==='progresso'){$('lessonsView').hidden=true;$('progressView').hidden=false;progress()}else{current=name;$('lessonsView').hidden=false;$('progressView').hidden=true;refresh()}document.querySelectorAll('.nav-item').forEach(b=>b.classList.toggle('active',b.dataset.view===name));window.scrollTo({top:0,behavior:'smooth'})}$('search').oninput=render;$('markVisible').onclick=()=>{const d=doneSet(),q=$('search').value.toLowerCase().trim();(data[current]||[]).forEach(v=>{if(!q||v.title.toLowerCase().includes(q))d.add(v.id)});save(current,d);refresh()};$('clearDone').onclick=()=>{save(current,new Set());refresh()};document.querySelectorAll('.nav-item').forEach(b=>b.onclick=()=>view(b.dataset.view));$('backToLessons').onclick=()=>view(current);let deferredPrompt=null,installBtn=$('nativeInstall');window.addEventListener('beforeinstallprompt',e=>{e.preventDefault();deferredPrompt=e;installBtn.hidden=false;installBtn.classList.add('install-ready')});installBtn.onclick=async()=>{if(!deferredPrompt)return;deferredPrompt.prompt();await deferredPrompt.userChoice;deferredPrompt=null};window.addEventListener('appinstalled',()=>installBtn.hidden=true);if('serviceWorker'in navigator)window.addEventListener('load',()=>navigator.serviceWorker.register('./sw.js').catch(()=>{}));load().catch(()=>{$('list').innerHTML='<tr><td colspan="4">Não foi possível carregar as playlists.</td></tr>'});